# CCCC 实战评估报告 v32

> 日期：2026-05-14
> Workflow ID：kanban-v1
> Group ID：g_141e439f6763
> 总耗时：~13 min（18:24:15 → 18:37:03）
> 任务数：4（T1→T2+T3→T4）

---

## 评分摘要

| 维度 | 分数 | 关键发现 |
|------|------|----------|
| 结果指标 | 4/5 | 0 CRITICAL, 8 WARN（边界条件+测试深度），tsc 通过 |
| 过程指标 | 5/5 | 10/10 必检项全部 positive，workflow.completed 事件确认 |
| 体验指标 | 4/5 | Foreman 自评 4.2/5，0 手工干预（除 plan 编写），100% worker 可靠 |
| **综合** | **4.3/5** | **历史新高**（超 v28 的 4.2/5） |

---

## 一、结果指标（4/5）

**审查者**：Codex（对抗式代码审查，read-only sandbox）

### CRITICAL (0)
无。

### WARN (8)

| # | 文件:行号 | 问题 | 影响 |
|---|----------|------|------|
| W1 | backend/app/models.py:63 | `title` 只做 `min_length=1`，允许纯空白标题 `"   "` | 数据质量边界条件 |
| W2 | backend/app/routers/cards.py:130 | `move_card` 不校验源/目标 column 是否属于同一 board | 跨 board 迁移边界 |
| W3 | backend/tests/test_api.py:289 | FK 约束测试通过 ORM cascade 而非 raw SQL，PRAGMA 关闭也能通过 | FK 回归漏检 |
| W4 | backend/tests/test_api.py:230 | move 测试缺少 position > count、负数 position 边界 | 边界校验回归漏检 |
| W5 | frontend/src/BoardView.tsx:103 | 拖到已有 card 上用 `overCard.position`，不能表达"插到尾部" | 拖拽排序语义不完整 |
| W6 | frontend/src/App.tsx:33 | 删除后 `loadBoards()` 未 await，promise 未处理 | 状态一致性 |
| W7 | frontend/src/App.tsx:6 | App 组件超过 50 行 | 代码指标 |
| W8 | frontend/src/BoardView.tsx:20 | BoardView 超 50 行，拖拽+CRUD 混在一起 | 代码指标 |

### OK (21)
- backend/app/database.py: PRAGMA foreign_keys=ON 正确注册，无 SQL 拼接
- backend/app/main.py: CORS 允许 localhost:5173，路由挂载完整
- backend/app/routers/boards.py: CRUD + 删除后 position 归一化正确
- backend/app/routers/columns.py: CRUD + 按 board 查询 + position 归一化正确
- backend/tests/conftest.py: 真实 TestClient + SQLite，无 mock 伪覆盖
- backend/tests/test_integration.py: board→column→card→move→delete 主流程覆盖
- frontend/src/api.ts: URL/method/payload 与后端 schema 匹配
- frontend/src/CardItem.tsx: 无 dangerouslySetInnerHTML，React 默认转义
- frontend/src/Column.tsx: droppable + SortableContext 使用正确
- 其余 12 个文件均 OK

### 验证执行
- `npx tsc --noEmit`: **通过**
- `python -m pytest -v`: 沙箱限制未执行（非代码问题）；workflow 内执行时 25 tests passed

### 评分依据
0 CRITICAL，8 WARN 但均不影响主流程（边界条件+代码指标），有 25 个测试覆盖核心场景。符合 4/5 标准。

---

## 二、过程指标（5/5）

**审查者**：Codex（工作流日志分析，70 行 ledger）

### 10 项必检结果

| # | 检查项 | 判定 | 证据 |
|---|--------|------|------|
| 1 | ralph validate 执行 | 有证据 | 3 次验证：2 次 failed（18:21:14, 18:22:11）→ 1 次 passed（18:23:39，0 errors 17 warnings） |
| 2 | 按批次提交 | 有证据 | 3 个 batch：B1=[T1], B2=[T2,T3]-auto, B3=[T4]-auto，符合 DAG 拓扑 |
| 3 | claimed_paths 精确 | 有证据 | T1=["backend/app/","backend/tests/__init__.py",...], T2=["backend/tests/test_api.py",...], T3=["frontend/",...], T4=["backend/tests/test_integration.py"]，无 "/" |
| 4 | verification.checks 真实执行 | 有证据 | T1: compile(757ms)+test(638ms)+6项语义检查; T2: compile(387ms)+test(1076ms,23passed); T3: compile(1863ms); T4: backend-all-tests(1171ms,25passed)+frontend-compile(263ms)。全部有 duration_ms>0 和 stdout |
| 5 | depends_on 强制执行 | 有证据 | T2/T3 在 T1 verification_passed 同秒启动；T4 等 T2+T3 都完成才启动（AND 语义正确） |
| 6 | 正确分配 worker | 有证据 | T1/T2/T4→worker(codex), T3→worker-fe(claude)，类型匹配 |
| 7 | 使用 task complete | 有证据 | 4 个 task_reported_completed 事件，含 evidence(agent_id, duration_seconds, changed_files) |
| 8 | 无 stall/failure/retry | 有证据(无异常) | 零 failed/stalled/timeout/retry 事件 |
| 9 | 自动化比例 | 有证据 | 4/4 = 100% 自动闭环 |
| 10 | 元数据传递 | 有证据 | claimed_paths/verification.checks/provides/consumes/depends_on/covers_flows 全部从 plan.yaml 正确传递 |

### 额外检查

| 检查项 | 结果 | 证据 |
|--------|------|------|
| workflow.completed 事件 (RO-77) | **通过** | 18:37:03 `workflow.completed` completed_count=4, failed_count=0 |
| auto-dispatch | **通过** | B2/B3 含 `-auto` 后缀，orchestrator 自动发送 [Foreman Assignment] 给 worker |
| DAG gating | **通过** | T1→(T2\|\|T3)→T4 严格按依赖时序释放 |

### 评分依据
10/10 必检项全部 positive + 3 项额外检查全通过。满分 5/5。

---

## 三、体验指标（4/5）

**审查者**：Foreman 自身（WORKFLOW_EVALUATION.md，133 行）

### 正面反馈
- **Ralph validate**: 5 轮迭代虽多但每轮反馈清晰可执行，`--show-schema` 是关键
- **Auto-dispatch + DAG gating**: "Worked flawlessly"，零手工 `cccc send`
- **Verification gate**: 所有 4 个任务通过 shell + agent 语义检查
- **Heartbeat**: Worker 定期发送心跳，无误报
- **Worker 可靠性**: codex 3/3, claude 1/1 = 100%
- **Assignment map**: `--assignment-map` 正确路由后端→codex、前端→claude

### 负面反馈
- **validate 迭代成本**: 5 次才 0 error（provides/consumes 格式不明显、level 必填但文档示例用 mode、type 不支持 test）
- **Greenfield 警告噪音**: 17 warnings + 20 hints，W_VERIFICATION_SHAPE_UNKNOWN ×12
- **Actor ID 默认行为**: 第二个 worker 创建时 ID 冲突

### 手工干预记录
- Plan 编写：手工（预期行为）
- Worker 创建：手工（预期行为）
- **任务分发**：全自动（0 手工干预）
- **验证执行**：全自动
- **批次推进**：全自动

### Foreman 自评：4.2/5

### 评分依据
少量摩擦（validate 迭代、warning 噪音），但核心机制全部正面反馈，0 手工干预执行阶段。符合 4/5。

---

## 四、交叉验证

| 场景 | 分析 |
|------|------|
| 结果 4 + 过程 5 | 一致：过程机制完美运行，结果 WARN 是边界条件（verification 语义检查无法覆盖所有边界场景），工作流已发挥最大效用 |
| 体验 4 + 过程 5 | 一致：摩擦仅在 plan 编写阶段（validate 迭代），执行阶段体验完美 |
| 结果 4 + 体验 4 | 一致：Foreman 未美化，结果 WARN 主要在边界条件而非主流程 |

**三维度高度一致**，无矛盾需解释。

---

## 五、与上一版对比（v32 vs v28 历史最高）

| 指标 | v28 | v32 | 变化 | 归因 |
|------|-----|-----|------|------|
| 结果 | 3/5 | 4/5 | +1 | 0 CRITICAL（v28 有 FK 测试伪覆盖唯一缺陷），验收标准引导质量 |
| 过程 | 5/5 | 5/5 | ±0 | 持续满分，v32 新增 workflow.completed 事件验证（RO-77） |
| 体验 | 4.5/5 | 4/5 | -0.5 | validate 迭代 5 次（v28 3 次），provides/consumes 新语法增加复杂度 |
| 综合 | 4.2/5 | **4.3/5** | **+0.1** | **历史新高** |

### 结果分提升归因
- v28 唯一缺陷（FK 测试伪覆盖）→ v32 仍存在（W3）但无 CRITICAL
- v32 验收标准更严格（含 position 重排、删除后重排），Worker 实现质量更高
- 0 CRITICAL 实现 bug（连续 v28→v32 保持）

### 体验分微降归因
- BP-2 (module_spec) + BP-4 (provides/consumes contract) 新能力增加 plan 复杂度
- provides/consumes 格式需要 `{name, kind}` 对象而非字符串，首次使用者需要多次迭代
- 建议：能力指南中增加更精确的 provides/consumes 示例

---

## 六、根因定位

### 已解决（本轮验证）
| 编号 | 问题 | 验证结果 |
|------|------|----------|
| RO-77 | workflow.completed 事件缺失 | **已修复** — 18:37:03 事件确认 |
| RO-78 | register_and_suggest retry workflow_id mismatch | **已修复** — 零 retry/error 事件 |
| RO-79 | verify command 语法错误未在 validate 检出 | **未直接验证** — 本轮无语法错误 |
| BP-2 | ModuleSpec 模型 + TaskRef 传递 | **已验证** — plan.yaml 使用 module_spec，元数据正确传递 |
| BP-4 | provides/consumes 静态验证 + 运行时 contract check | **已验证** — plan_validated 事件含契约验证 |
| BP-5 | batch_e2e_command 端到端验证 | **未直接验证** — Foreman 未使用此字段（W_BATCH_E2E_NO_COMMAND warning） |

### 未解决（已知）
| 编号 | 问题 | 状态 |
|------|------|------|
| RO-80 | challenge/agent 验证不可用时 fail-open | 待修复 |
| RO-81 | _check_cross_task_io_contracts 多上游误报 | 待修复 |

### 新发现
| 编号 | 问题 | 优先级 | 影响 |
|------|------|--------|------|
| UX-1 | provides/consumes 格式文档与 schema 不一致 | P2 | 体验（validate 迭代增加） |
| UX-2 | Greenfield 项目 W_VERIFICATION_SHAPE_UNKNOWN 大量重复 | P2 | 体验（warning 噪音） |
| UX-3 | E_VERIFICATION_TARGET_MISSING_FILE 在 greenfield 应为 warning | P2 | 体验 |

---

## 七、后续改进及对应实例

### [UX-1] provides/consumes 格式文档改进

**优先级**：P2
**影响维度**：体验
**预期分数变化**：体验 4→4.5

**触发实例**：
> v32 实战中，Foreman 用字符串格式声明 provides（如 `provides: "backend_api"`），被 validate 拒绝，需改为 `{name: "backend_api", kind: "api"}` 对象格式。5 次 validate 迭代中至少 2 次因格式问题。
> Foreman 反馈："`provides`/`consumes` format not obvious from docs (needed `{name, kind}` objects, not strings)."

**根因**：foreman-capability-guide.md 中 provides/consumes 示例不够精确，未明确展示完整 schema。

**改进方案**：在 foreman-capability-guide.md 中增加 provides/consumes 完整示例（含 from_task 字段），与 `ralph validate --show-schema` 输出一致。

**验收标准**：下一轮实战中，Foreman validate 迭代 ≤3 次。

---

### [UX-2] Greenfield 项目验证警告降噪

**优先级**：P2
**影响维度**：体验
**预期分数变化**：体验 +0.3

**触发实例**：
> v32 实战中，4 任务 plan 产生 17 warnings + 20 hints，其中 W_VERIFICATION_SHAPE_UNKNOWN ×12 占主要噪音。
> Foreman 反馈："17 warnings + 20 hints on a 4-task plan. Most were informational but created noise."

**根因**：验证规则不区分 greenfield 项目（无已有代码、语言未知）和已有项目。

**改进方案**：考虑 `--compact` 模式或 warning 分级显示（actionable vs informational）。

**验收标准**：同类 4 任务 plan 验证输出 ≤10 条 warning/hint。

---

## 八、综合评分：4.3/5

**历史新高**。主要进步：
1. 结果从 3→4：0 CRITICAL 实现 bug，验收标准驱动的质量保障持续有效
2. 过程持续满分：10/10 + workflow.completed 事件（RO-77）+ DAG gating AND 语义 + 100% 自动化
3. 体验稳定 4 分：auto-dispatch + verification 真实执行 + 零手工干预执行阶段

**瓶颈转移**：从"工作流机制是否工作"→"计划编写体验优化"（validate 迭代成本、warning 噪音）。核心机制已成熟。

---

## 版本历史评分追踪

| 版本 | 日期 | 结果 | 过程 | 体验 | 综合 | 关键改进 |
|------|------|------|------|------|------|----------|
| v28 | 2026-05-13 | 3/5 | 5/5 | 4.5/5 | 4.2/5 | 历史最高(当时); 0 CRITICAL; 过程满分; 100%自动分发 |
| v29 | 2026-05-13 | 2/5 | 4/5 | 4/5 | 3.3/5 | codex 5/5 stall; RO-77/78/79 新发现 |
| **v32** | **2026-05-14** | **4/5** | **5/5** | **4/5** | **4.3/5** | **新历史最高**; RO-77确认修复; 0 CRITICAL; 过程满分; 100%自动化; 瓶颈转向plan编写体验 |
