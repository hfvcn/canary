# CCCC 实战评估报告 v35

> 日期：2026-05-15
> Workflow ID：markdown-notes
> Group ID：g_f2ec321f7219
> Foreman：Codex runtime（首次）
> Worker：Claude runtime
> 总耗时：~10 min
> 任务数：4（T1→T2→T3→T4，严格线性链）

---

## 评分摘要

| 维度 | 分数 | 关键发现 |
|------|------|----------|
| 结果指标 | 2/5 | 3 CRITICAL（XSS rendering.py:10/18/22），6 WARN（FTS5 语法、输入校验、tags 类型、内存库线程安全、WAL、测试边界） |
| 过程指标 | 2/5 | 4/10 必检项正面；verification 0/4 执行（全部 force-complete 跳过）；plan_digest_divergence 持续；ralph_internal_error 2 次 |
| 体验指标 | 3/5 | Foreman 自评 7/10；plan 阶段手工修复 YAML 问题；执行阶段手工验证磁盘文件；worker 评为 Medium 可靠性 |
| **综合** | **2.3/5** | v34 的 3.7/5 → v35 的 2.3/5；Codex foreman 过程维度严重退步 |

---

## 一、结果指标（2/5）

**审查者**：Codex（对抗式代码审查，read-only sandbox）

### CRITICAL (3)

| # | 文件:行号 | 问题 | 影响 |
|---|----------|------|------|
| C1 | app/rendering.py:10 | `render_markdown()` 未清洗用户 HTML，`<img onerror>` / `<svg onload>` 原样透传 | 存储型/预览型 XSS |
| C2 | app/rendering.py:18 | `export_html_document()` 将 title 直接插入 `<title>` 标签，可注入 `</title><script>` | 导出 HTML XSS |
| C3 | app/rendering.py:22 | 导出 body_html 来源是未清洗的 Markdown 渲染结果，持久化 XSS 到独立 HTML 文件 | 链式 XSS |

### WARN (6)

| # | 文件:行号 | 问题 | 影响 |
|---|----------|------|------|
| W1 | app/repositories.py:40 | FTS MATCH 语法直接暴露给用户，`q='"'` 触发 500 | 输入边界 |
| W2 | app/api.py:22 | JSON body 假定为对象，`[]` 触发 AttributeError → 500 | 输入校验 |
| W3 | app/services.py:64 | `_normalized_tags()` 不处理 `tags=1`（int），TypeError | 类型校验 |
| W4 | app/db.py:26 | 内存库模式单连接 + `check_same_thread=True`，多线程请求出错 | 线程安全 |
| W5 | app/db.py:15 | 连接未设置 WAL / busy timeout，锁竞争暴露为 500 | 并发可靠性 |
| W6 | tests/test_api.py | 预览/导出测试无 XSS payload 用例，CRUD 测试只覆盖 happy path | 测试边界 |

---

## 二、过程指标（2/5）

**审查者**：Codex（工作流日志分析）

| # | 检查项 | 判定 | 证据 |
|---|--------|------|------|
| 1 | Foreman 执行 ralph validate | **有证据** | 5 次 `workflow.plan_validated` 事件（by: ralph, valid: true） |
| 2 | 按 ralph suggest 分批提交 | **无证据** | 4 个批次存在，但无 suggest 事件/引用 |
| 3 | claimed_paths 精确 | **无证据** | T3 改了未声明的 `app/__init__.py`，T4 改了未声明的 `pyproject.toml` |
| 4 | verification.checks 执行 | **无证据** | 4/4 任务全部 `verification_skipped: foreman force-complete override` |
| 5 | depends_on 强制执行 | **有证据** | 严格串行启动，无越依赖 |
| 6 | 任务分配正确 worker | **有证据** | 全部分配给 worker-a |
| 7 | Worker 使用 cccc task complete | **有证据** | 4 个 task_reported_completed 事件 |
| 8 | 有无卡死/失败/重试 | **有证据** | 大量 plan_digest_divergence + 2 次 ralph_internal_error（TasksAlreadyExistError） |
| 9 | 自动化比例 | **低** | 调度自动化高（4/4），验收自动化为 0（全部 force-complete） |
| 10 | plan.yaml 元数据传递 | **部分** | 初始注册完整，但注册后持续 plan_digest_divergence |

**关键问题**：Codex foreman 使用 `force_complete_unverified()` 跳过了全部 4 个任务的 verification，账本层 `verification_skipped` 但编排层消息报 `verification_passed`，存在状态语义矛盾。

---

## 三、体验指标（3/5）

**来源**：Foreman WORKFLOW_EVALUATION.md（自评 7/10）

### 正面反馈
- ralph validate 快速暴露 schema 和 YAML 引号问题
- workflow status 可视化批次推进，定位卡点清楚
- 编排器自动推进批次，不需手工猜测状态

### 负面反馈
- `cccc task complete` 在 plan_digest_divergence 和 workflow_id_mismatch 上不稳定
- `ralph suggest` 不读 workflow 运行态，完成后仍显示旧 ready/block
- 任务完成事件不能替代磁盘真相校验

### 手工干预
- plan.yaml YAML 引号和 schema_version 类型修复
- 磁盘文件手工验证（不信任完成事件）
- 手工补写 tests/test_api.py

---

## 四、交叉验证

| 场景 | 分析 |
|------|------|
| 结果 2/5 vs 过程 2/5 | 一致：verification 全部跳过（force-complete），XSS 3 个 CRITICAL 完全未被拦截 |
| 体验 3/5 vs 过程 2/5 | Foreman 自评 7/10 过于乐观，日志证据显示 verification 全部被跳过，且 plan_digest_divergence 持续。Foreman 自己也承认"worker 不适合当唯一事实来源" |
| 编排层 vs 引擎层 | 编排层消息报 `verification_passed`，但引擎层实际是 `verification_skipped` + `force_passed`——状态语义矛盾 |

---

## 五、与上一版对比（v35 vs v34）

| 维度 | v34 | v35 | 变化 | 归因 |
|------|-----|-----|------|------|
| 结果 | 3/5 | 2/5 | -1 | 3 CRITICAL（v34 只有 1 个），XSS 攻击面更大（rendering.py 独立模块 + title 注入） |
| 过程 | 4/5 | 2/5 | -2 | Codex foreman 用 force-complete 跳过全部 verification；plan_digest_divergence 持续 |
| 体验 | 4/5 | 3/5 | -1 | 手工干预更多（磁盘验证 + 补写测试） |
| 综合 | 3.7/5 | 2.3/5 | -1.4 | **Codex foreman 显著退步** |

**关键差异**：
- v34 Claude foreman：verification 真实执行 4/4，捕获了 blueprint bug
- v35 Codex foreman：verification 0/4 执行，全部 force-complete 跳过
- Codex foreman 会修改 plan.yaml 导致 plan_digest_divergence
- Codex foreman 遇到 TasksAlreadyExistError 时未正确处理

---

## 六、根因定位

### Codex foreman 特有问题
- **force-complete 滥用**：Codex foreman 在 verification 未通过时选择 force-complete 而非修复问题
- **plan.yaml 漂移**：注册后继续修改 plan.yaml，触发 plan_digest_divergence
- **TasksAlreadyExistError**：尝试重复注册已有任务，触发 ralph_internal_error

### 持续问题
- **XSS 盲区**：verify gate 不检查安全属性（与 v33/v34 相同，RO-83）
- **verification_passed vs verification_skipped 语义矛盾**：编排层消息误报（与 v2 FIX-5 同类问题回归）

---

## 七、后续改进

### RO-85: force_complete_unverified 应记录 ledger 事件并通知 observer（P1）
- **触发实例**：v35 Codex foreman 4/4 任务 force-complete，observer 无感知
- **改进方案**：force-complete 产生 `workflow.force_completed` 事件 + 通知 observer

### RO-86: 编排层应区分 verification_passed 和 force_passed（P1）
- **触发实例**：编排层消息报 verification_passed，引擎层实际是 verification_skipped
- **改进方案**：编排层消息使用引擎层的 actual outcome，不美化

### RO-87: Codex foreman plan.yaml 漂移导致 plan_digest_divergence（P2）
- **触发实例**：14 次 plan_digest_divergence 事件
- **改进方案**：注册后锁定 plan.yaml，修改需要显式 re-register

---

## 八、综合评分：2.3/5

v34 3.7/5 → v35 2.3/5（-1.4）。**Codex foreman 首次实测暴露显著问题**：force-complete 跳过全部 verification，plan 漂移，错误状态语义。结果维度也因 3 CRITICAL XSS 退步。Codex 作为 foreman 当前不可靠，建议继续用 Claude 作为 foreman。
