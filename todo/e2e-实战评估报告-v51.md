# E2E 实战评估报告 v51

> 日期：2026-06-03
> 项目：FastAPI 安全文件共享服务（JWT+RBAC+文件上传+分享链接+审计日志）
> 团队：lead(foreman,claude) + worker-a(claude) + worker-b(claude) + security-reviewer(claude)
> 任务数：9（T01~T09），全部完成
> 总耗时：~32min（13:46→14:18）
> 测试数：100 passed / 0 failed / 107 warnings in 42s

## 评分摘要

| 维度 | 评分 |
|------|------|
| 结果 | 4.0/5 |
| 过程 | 4.0/5 |
| 体验 | 4.0/5 |
| **综合** | **4.0/5** |

## 交叉验证：v51 修复项逐项确认

### ✅ 已验证生效

| ID | 标题 | 验证证据 |
|----|------|---------|
| FL-55 | 独立验证告警不可压制 | validate 输出含 `W_CRITICAL_FLOW_WORKER_ONLY_VERIFICATION [T03_ROUTERS]`，该告警出现在 warnings 中未被 suppress |
| FL-56 | security sign-off gate | validate 输出含 `W_SIGNOFF_STRUCTURE_WEAK [T09_SECURITY_REVIEW]` 对 authentication_flow 和 authorization_flow 两条 flow 分别报警 |
| FL-58 | override 与纯通过分类 | WORKFLOW_EVALUATION.md 含 `result_breakdown: passed=6, overridden=1, assumption_based=0, independently_reviewed=2`，T01 明确归类为 overridden 而非 passed |
| FL-59 | ralph complete YAML 损坏 | plan.yaml 在 workflow 全程保持结构完整（foreman 多次 validate 无 YAML parse error），未触发 YAML 损坏 |
| FL-60 | ledger failed 标记未清 | T01 verification_failed→foreman_override→T02 started，T02 retry 后 verification_passed→T03 started，状态转换正确无死锁 |
| FL-62 | file::function entrypoint 语法 | validate 输出无 entrypoint 格式误报（v50 有 5 个误报错误，本轮 0 个） |
| RV-30 | task paths 越出 plan_scope | validate 第一次报 2 errors 含路径越界类错误，foreman 修复后 0 errors 通过 |
| UX-22 | SUSPICIOUS 浅检查误报 | T09 verification 中 `no_hardcoded_jwt: passed (10ms) - [SUSPICIOUS: completed in 10ms]` 标记但未阻止通过——SUSPICIOUS 标签仅为 advisory，不再误报阻断 |

### ⚠️ 部分生效 / 需进一步观察

| ID | 标题 | 状况 |
|----|------|------|
| FL-57+FL-50 | randomized check | WORKFLOW_EVALUATION.md 中 `test_stats_reliable: true` 但实际无 pytest-randomly 插件。Codex review 指出该结论证据不足。validate 未报 randomization 相关告警——可能因项目无 randomized check 声明而未触发规则 |
| FL-61 | aegis_evidence_card 自相矛盾 | 本轮 T01 override 的 evidence_card 格式正确（有 reason+evidence），但未遇到 evidence 与 outcome 矛盾的场景 |
| RV-31 | 安全 flow 被 suppress_flows 降级 | 本轮 foreman 未使用 suppress_flows / suppress_codes，故规则未被触发 |
| RV-32 | contract kind 不一致 | 本轮 provides/consumes 契约类型一致，未触发 `E_CONTRACT_KIND_MISMATCH` |

### 关键安全验证

| 检查点 | 结果 |
|--------|------|
| **RV-29 注册提权** | ✅ T04 auth tests 含 "role=admin in body ignored (2 variants)"，runtime 验证注册塞 role=admin 被忽略，落库为 member |
| **W_AUTH_PRIVILEGED_ROLE_FIELD** | ✅ validate 报警 `W_AUTH_PRIVILEGED_ROLE_FIELD [T03_ROUTERS, T04_AUTH_TESTS, T08_E2E, T09_SECURITY_REVIEW]` |
| **JWT 无默认密钥** | ✅ app/config.py 纯 env var 读取，RuntimeError if missing，SECURITY_REVIEW.md Finding 1 确认 |
| **RBAC 隔离** | ✅ T05 RBAC tests 13 cases：member 不能访问 admin 端点/他人文件 |
| **分享链接安全** | ✅ T06 share tests：过期返回 410，密码错误 403，撤销后不可访问 |

## workflow 执行时间线

| 时间 | 事件 |
|------|------|
| 13:46 | 任务发送给 foreman |
| 13:47 | foreman 读完 capability guide，开始写 plan |
| 13:49 | 第一次 validate 失败（2 errors） |
| 13:51 | 修复后 validate 通过（0 errors, 46 warnings） |
| 13:52 | 创建 security-reviewer（非执行者角色 ✅） |
| 13:52 | 9 tasks 注册，T01 → worker-a |
| 13:54 | T01 完成但 compile_check 失败（缺 JWT env） |
| 13:57 | foreman override T01，T02 → worker-a |
| 14:02 | worker-a stall 10min → T02 fail → retry → worker-b |
| 14:04 | T02 验证通过，T03 → worker-a |
| 14:09 | T03 通过，并行启动 T04(worker-a) + T05(worker-b) + T06(security-reviewer) |
| 14:10 | T04 通过，T07 → worker-a |
| 14:11 | T07 通过 |
| 14:15 | T05 + T06 通过 |
| 14:17 | T08 E2E 通过，T09 → worker-a |
| 14:18 | T09 Security Review 通过，**workflow.completed** |

## Codex review 发现

### Results review (verdict=needs_revision, confidence=4)
1. **[high] RV-29 is_admin 注入未覆盖** — plan 声明 forbidden_flow 要求同时拒绝 role=admin 和 is_admin，但 auth tests 只覆盖 role=admin 和 role=superadmin，无 is_admin 测试用例
2. **[high] FL-57 test_stats_reliable** — WORKFLOW_EVALUATION 写 true 但无 pytest-randomly 插件，当前环境无法独立复核，更应为 false
3. **[medium] plan.yaml 410 vs 404 漂移** — T08 目标写 revoke 后 410，但实现和 E2E 测试以 404 为准
4. **[medium] independently_reviewed=2 缺映射** — 文档未标明哪两个任务被计为 independently_reviewed
5. **[medium] SECURITY_REVIEW.md 范围过大** — 声称"源码无硬编码默认值"但 conftest.py 有测试用 setdefault

### Process review (verdict=revise, confidence=4)
1. **FL-55 ✅** — W_CRITICAL_FLOW_WORKER_ONLY_VERIFICATION 出现在 validate 输出
2. **FL-56 ✅** — W_SIGNOFF_STRUCTURE_WEAK 对两条安全 flow 报警
3. **UX-20 ✅** — worker-a stall 10min 被正确检测，reassigned to worker-b
4. **并行度好** — T04/T05/T06 三路并行
5. **foreman override 留痕** — T01 有明确 reason 和 evidence

## v51 E2E 新发现

### FL-63（WORKFLOW_EVALUATION 中 test_stats_reliable 自动化判定缺失，P2）
foreman 写 `test_stats_reliable: true` 但实际未使用 pytest-randomly。CCCC 系统应自动检测是否有 randomized 插件标志，而非依赖 foreman 自述。FL-57 规则（`_check_randomization_capability`）在 validate 阶段对 plan 检查，但 WORKFLOW_EVALUATION.md 的 test_stats_reliable 字段缺乏 runtime 校验。

### FL-64（is_admin 注入边界未被 forbidden_flow 强制覆盖，P2）
plan forbidden_flows 声明 "MUST NOT accept role=admin or is_admin fields"，但 validate 和 verification gate 均未检测实际测试是否覆盖了所有声明的注入向量。RV-29 的 validate 规则 `W_AUTH_PRIVILEGED_ROLE_FIELD` 检测到了 identity surface，但不检查测试是否覆盖了所有声明的字段名。

### FL-65（independently_reviewed 分类缺任务级映射，P3）
WORKFLOW_EVALUATION.md result_breakdown 声明 independently_reviewed=2 但未标明具体哪两个任务。审计追溯困难。

### UX-23（WORKFLOW_EVALUATION.md 初始生成为占位符后补充实质，P3）
WORKFLOW_EVALUATION.md 第一次生成时大部分章节为"待 foreman 补充"占位符（926B），foreman 后续补充为完整内容（5007B）。与 v50 UX-18b 类似但方向不同——这次是从占位符到实质内容的补充过程成功了。

### RV-39（validate 不检测 plan 目标与实现状态码漂移，P3）
T08 plan 目标声明 revoke 后返回 410 Gone，但实现为 404。validate 和 verification 均未检测此类规格与实现的漂移。

## 改进建议

1. **runtime test_stats_reliable 校验**（FL-63）：在 workflow completion 阶段自动检测是否有 pytest-randomly 或等效随机化证据，而非依赖 foreman 自述
2. **forbidden_flow 注入向量覆盖检测**（FL-64）：validate 或 verification 阶段检测测试是否覆盖了 forbidden_flow 声明的所有注入字段名
3. **result_breakdown 任务映射**（FL-65）：要求 WORKFLOW_EVALUATION 的 result_breakdown 含逐任务分类
4. **目标-实现状态码一致性检测**（RV-39）：validate 或 verification 阶段比对 plan 中声明的 HTTP 状态码与测试中断言的状态码

## v53 新实现检查（FL-42 + MSE-1~6 + AgentFlow M0~M6）

> v53 solve flow 完成了 22 tasks 的代码实现。以下为本轮 E2E 中可观察到的相关行为。

| 模块 | 观察 |
|------|------|
| FL-42 评价闭环 | WORKFLOW_EVALUATION.md 自动生成 ✅（workflow.completed 后产出），含 result_breakdown 和 test_count_actual |
| MSE-1/2 注册与成本 | foreman 固定使用 claude（opus），未触发模型选择——本轮所有 actor 均为 claude |
| MSE-3 acquire/release | 未直接观测——需要 AgentFlow 引擎路径才能触发 |
| MSE-4/5 trace+评分 | 未直接观测——AttemptLink/TraceBridge 需要 AF 引擎 |
| MSE-6 进化闭环 | 未直接观测——TunedAgentVersion/promotion 需要跨 E2E 累积 |
| AgentFlow M0~M6 | 未直接观测——当前 workflow 走 legacy 执行引擎，AF 引擎切换需要配置启用 |

**结论**：v53 的 22 tasks 实现了代码和测试，但 AF 引擎组件**从未被 WorkflowOrchestrator 主路径调用**——组件独立存在，orchestrator 仍走老路径。T21-T22 声称"全链路集成"但实际只在测试文件中独立测试了各组件。这暴露了一个系统性验证盲区：**组件级测试通过≠集成到主路径**（→FL-66/RV-48）。

### v52 RV-34 验证状态

⚠️ 部分验证：`W_SIGNOFF_STRUCTURE_WEAK` 在 T09 对两条安全 flow 报警——RV-34 代码路径被执行。但 T09 signoff 本身就弱，无法单独确认 grep 排除和路径引用的强/弱区分逻辑是否真正生效（需要一个有正确 signoff 结构的任务才能验证不误报）。
