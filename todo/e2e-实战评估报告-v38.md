# E2E 实战评估报告 v38

## 评分摘要

| 维度 | 评分 | 说明 |
|------|------|------|
| Plan 质量 | 4/5 | 6 task 结构清晰，contracts/depends_on 正确，2 次 validate 迭代后通过 |
| Worker 执行 | 5/5 | 111 tests, 92% coverage, 6/6 tasks 完成, 仅 1 次 retry (api-routes bp 命名) |
| Verify gate 准确性 | 4/5 | 修复后 input_robustness 不再误报，api-routes 的 fail 是真实问题 |
| Workflow 协调 | 4/5 | 3 batch 自动 dispatch，依赖阻断正确，单 worker 串行执行 |
| 安全深度 | 4/5 | 33 安全测试，FTS5 注入/timing-safe auth/input validation 全覆盖 |
| **综合** | **4.2/5** | 修复后首次全流程无误报完成 |

## 交叉验证

### Foreman 评估 vs Codex 审查对比

| 项目 | Foreman 自评 | Codex Process Review | 差异 |
|------|-------------|---------------------|------|
| Plan 质量 | 8/10 | 确认结构良好 | 一致 |
| Worker 可靠性 | 9/10 (1 retry) | 确认自修复能力强 | 一致 |
| Verify pipeline | 8/10 | 确认 checks 有效 | 一致 |
| 安全覆盖 | 9/10 | sandbox 限制无法独立跑 pytest 验证 | 未完全独立验证 |

### 关键发现

1. **input_robustness gate 修复验证通过** ✅
   - 修复前：所有 task 因 "lacks malformed input coverage" 误报失败（连续 3 轮 E2E 复现）
   - 修复后：首个 task verification_passed，无 false positive
   - 根因：test files 属于下游未执行 task，物理上不存在时错误判定为 gap

2. **api-routes 的 verification_failed 是真实问题** ✅
   - blueprint 导出命名不匹配（`api` vs `bp`），compile check 正确捕获
   - Worker 自主修复，无需 foreman 干预

3. **plan validation 迭代次数合理**
   - 2 次失败 → 修复 aegis.intent enum + entrypoint 格式 → 通过
   - 说明 ralph validate 的报错信息可行动

4. **单 worker 瓶颈**
   - Batch 1 有 3 个独立 task（core-models, auth-module, input-validation），串行执行
   - 多 worker 可将 Batch 1 从 ~3min 压缩到 ~1min

## 测试数据

- Workflow ID: flask-fts5
- Group: g_6640162b9e8c
- 总耗时: ~13 min（plan 5min + execution 6min + evaluation 2min）
- Tasks: 6/6 completed
- Tests: 111 passing, 92% coverage
- Retries: 1 (api-routes, worker 自修复)
- Manual interventions: 0

## 修复验证

本轮 E2E 验证了以下修复:

### RO-96/97: input_robustness_smoke false positive (P1)
- **状态**: ✅ 已修复
- **修复内容**:
  1. `verification_gate.py`: `_resolve_input_robustness_plan_data` 合并完整 plan 的 tasks（跨 task test file 发现）
  2. `security_scan.py`: `_any_test_covers_robustness` 当所有声明的 test files 都不存在时（下游 task 未执行），不阻断当前 task
- **验证**: verification_passed，6/6 tasks 无误报完成

## 改进建议

1. **`--greenfield` flag for ralph validate**: 新项目应自动 suppress W_VERIFICATION_TARGET_MISSING
2. **多 worker 并行**: 独立 batch 内的 tasks 应分配给不同 workers
3. **SUSPICIOUS 阈值调整**: grep-based checks <50ms 是正常的，不应标记 suspicious
4. **Entrypoint 格式文档**: `file::symbol` vs `file` 格式需在 guide 中明确说明
