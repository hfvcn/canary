# CCCC 工作流问题清单 v3 (未解决)

> 日期：2026-04-25（Codex 复核更新）/ **2026-05-14 v32 E2E 后更新**
> 状态：Batch A~H + D-fix + EFH-fix + E2E-fix 全部完成。最新 **E2E v32 综合 4.3/5（历史新高）**。
> 全量版本：[问题清单-full.md](./问题清单-full.md)
> Ralph 专项：[问题清单-v5-ralph.md](./问题清单-v5-ralph.md)（含 BP 蓝图偏移分析）
> 蓝图对齐：[工作流蓝图.md](./工作流蓝图.md) v0.3（两阶段拆分 + Ralph Agent）
> 评估报告：[e2e-实战评估报告-v27.md](./e2e-实战评估报告-v27.md)（最新）
>
> **本文档只保留未解决的问题。已修复的条目保存在 full 版本中。**

## 当前状况速览（2026-05-13 更新）

### v14→v32 进展
- **32 轮 E2E 实战**，综合分从 1.8/5 提升到 **4.3/5（历史新高）**
- **引擎层已成熟**：DAG 门控 + auto-dispatch + verification gate + stall detection + workflow.completed 事件连续稳定
- **本清单剩余 1 个未修复**：FIX-E2E-7 / RO-57（re-submit workflow_id 冲突）——RO-78 已部分解决（retry 路径修复）

### 测试状态
2628 tests pass, 0 failed, 120 skipped. E2E v32 综合 4.3/5（结果 4, 过程 5, 体验 4）。

### 下一步应该做什么
1. **UX-1/2/3 (P2)** — plan 编写体验优化（provides/consumes 文档、greenfield 警告降噪）→ 详见 [问题清单-v5-ralph.md](./问题清单-v5-ralph.md)
2. **RO-80/81 (P2-P3)** — challenge fail-open + 多上游误报
3. **Phase 5（ARCH-14~16）** — Ralph Agent + 两阶段拆分 + verification_mode

---

## 待解决

### ~~FIX-E2E-3~~ Worker heartbeat — ✅ v23+ E2E 验证通过，移入 full

> v23/v26/v27 E2E 中 worker heartbeat 正常工作：v26 worker-be heartbeat 频繁且描述性强（"创建 backend 目录"→"运行 pytest"）；v27 Codex worker 有 heartbeat 但无文件产出（stall detection 依赖 heartbeat 工作正常）。

### ~~FIX-E2E-5~~ ralph validate 审计事件 — ✅ v15+ E2E 验证通过，移入 full

> v15~v27 所有 E2E ledger 中均出现 `plan_validation_failed` / `plan_validated` 事件，时戳早于 `batch_registered`。Foreman 自动传递 group 参数（通过 `cccc use` 设置 active group 后 CLI 自动携带）。

### ~~FIX-E2E-8~~ Terminal failed 无法 re-complete — ✅ v19+ 已修复，移入 full

> `workflow retry TASK_ID` 已实现（RO-47），将 failed task 重置为 ready 并重新分配。v19~v27 多轮 E2E 中 retry 正常工作（v26 T1 retry、v27 T2 retry）。

### FIX-E2E-7 / RO-57 re-submit 创建竞争 workflow_id（P1）

> **来源**：v14 E2E（FIX-E2E-7）+ v22 E2E（RO-57 重现）
- **严重度**：High — 单独 `cccc workflow submit` 已有 task 时创建子 workflow-id，`cccc task complete` 报 `workflow_id_mismatch`
- **现状**：v22 仍重现（RO-57）。后续 E2E 中 Foreman 学会使用 `workflow retry` 代替 re-submit 绕过，但底层问题未修
- **验收标准**：re-submit 已有 task 时重用原 workflow_id，或拒绝并提示使用 retry

### ~~ARCH-5~~ --worker-prompt — ✅ v14 验证通过，已移入 full

---

## 蓝图对齐（Phase 5，待实施）

> 依赖：Batch D~H 已完成，蓝图能力建立在偏移已修正的基础上
> 详见：[工作流蓝图.md](./工作流蓝图.md) v0.3 + [问题清单-v5-ralph.md](./问题清单-v5-ralph.md)

### ARCH-14 Ralph Agent 层实现（P5）→ 对应 RA-1 + RA-2
### ARCH-15 两阶段拆分对齐（P5）→ 对应 RA-4
### ARCH-16 INV-7 + verification_mode 分流（P5）→ 对应 RA-3

---

## 执行批次计划（2026-05-13 更新）

> ✅ = 代码已改 + 测试通过 + E2E 验证。

| Batch | 可验证结论 | 包含条目 | 状态 |
|-------|-----------|---------|------|
| **A** | 监控器在跑，violations 写入 ledger | ARCH-9 | ✅ commit 99d3346 |
| **B** | daemon start 只恢复 active group | FIX-11 | ✅ commit 99d3346 |
| **C** | 所有 assignment 经 Foreman | ARCH-1 + ARCH-2 + ARCH-3 + ARCH-6 | ✅ commit b9debf8 |
| **D** | Engine 状态转换完整 | ARCH-7 + ARCH-8 + ARCH-11 | ✅ |
| **E** | 非法转换被 Engine 拦截 | ARCH-12 + ARCH-9 block + FIX-16 | ✅ |
| **F** | Engine 是唯一真相源 | ARCH-4 + ARCH-10 + FIX-13/17/18 | ✅ |
| **G** | Ralph 规则增强 | RO-9n + RO-11 | ✅ |
| **H** | CLI 和审计完整 | FIX-15 + FIX-19 + FIX-20 | ✅ |
| **D-fix** | Codex 审查修复 | FIX-27 + FIX-28 | ✅ |
| **EFH-fix** | Codex 审查修复 | FIX-29 + FIX-30 + FIX-31 + FIX-32 | ✅ |
| **E2E-fix** | auto-dispatch fallback | FIX-E2E-1 | ✅ |
| **v14-fix** | Pool 复用 + completion + 自动推进 | FIX-E2E-2 + FIX-E2E-4 + FIX-E2E-6 + ARCH-5 | ✅ |
| **v15~v27** | 27 轮 E2E 迭代修复 | 70+ RO 条目 | 详见 [问题清单-v5-ralph.md](./问题清单-v5-ralph.md) |
| **I** | Ralph Agent + 两阶段拆分 | ARCH-14~16 + RA-1~4 | 待实施（Phase 5） |

**已关闭（v14 后新增）**：FIX-E2E-3（✅ v23 heartbeat）、FIX-E2E-5（✅ v15 validate 事件）、FIX-E2E-8（✅ v19 retry）、ARCH-5。
**仍未修复**：FIX-E2E-7 / RO-57（re-submit workflow_id 冲突）。

### 关键路径

```
A~H(done) → v14-fix(done) → v15~v27 E2E 迭代(done, 引擎层稳定)
→ FIX-E2E-7/RO-57(P1, 仍未修复)
→ I(Phase 5, Ralph 专项)
```
