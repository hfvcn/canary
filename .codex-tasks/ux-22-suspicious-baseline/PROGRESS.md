# Progress

## 2026-05-31

- Started task tracking for T12 / UX-22.
- Read `plan.yaml` T12, current `ralph_service.py` threshold logic, and existing suspicious duration tests.
- Updated `ralph_service.py` exemptions and added T12 baseline test coverage.
- Verified `tests/test_suspicious_baseline.py -v` passed, related suspicious-duration tests passed, and targeted `git diff --check` passed. `python -m ruff` was unavailable in the active Python environment.
