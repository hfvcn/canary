# T7 Randomization Verification

## Objective

Implement plan.yaml task T7: randomized workflow evaluation checks must verify pytest-randomly capability is actually present in the command.

## Scope

- Update `src/cccc/daemon/foreman/workflow_orchestrator.py`.
- Add `tests/test_randomization_check.py`.
- Validate with the T7 command from `plan.yaml`.

