# Progress

## 2026-06-02

- Located `CI Build` in `.github/workflows/ci.yml`.
- Confirmed automatic triggers are `push` to `main` and `pull_request`.
- Docs and release workflows are out of scope.
- Updated `CI Build` to use `workflow_dispatch` only.
- Verified no `push` or `pull_request` triggers remain in `.github/workflows/ci.yml`.
- Verified `.github/workflows/ci.yml` parses as YAML.
