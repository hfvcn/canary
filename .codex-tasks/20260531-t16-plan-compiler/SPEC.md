# T16 PlanCompiler

## Goal

Implement AF-M0b PlanCompiler skeleton for compiling CCCC `plan.yaml` tasks into an `ExecutionBundle`.

## Scope

- Add `src/cccc/agentflow/__init__.py`.
- Add `src/cccc/agentflow/plan_compiler.py`.
- Add `tests/agentflow/__init__.py`.
- Add `tests/agentflow/test_plan_compiler.py`.

## Validation

Run `python -m pytest tests/agentflow/test_plan_compiler.py -v`.
