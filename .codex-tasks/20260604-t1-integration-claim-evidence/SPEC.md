# T1 Integration Claim Evidence

## Goal

Implement and verify `_check_integration_claim_evidence(plan)` in `src/cccc/ralph/validation_rules/coverage.py`.

## Scope

- Align the rule behavior with the plan heuristic requirements.
- Keep the rule pure `Plan -> List[ValidationIssue]`.
- Add focused unit coverage in `tests/test_integration_call_evidence.py`.
