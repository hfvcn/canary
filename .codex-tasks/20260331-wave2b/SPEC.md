# Wave 2B

目标：按顺序完成 Ralph Wave 2B 的 4 个实现任务，并在每一步通过对应测试后再继续。

范围：
- T1: `filesystem_validator.py` 增加测试覆盖缺口检测并补测试
- T2: `validator.py` 增加 RV-4 降级与 RV-3 covers 可验证性检查并补测试
- T3: `validator.py` 增加 early integration checkpoint 检查并补测试
- T4: 新增 Wave 2B 集成测试并执行相关验证

约束：
- 严格按 `T1 -> T2 -> T3 -> T4` 执行
- 每一步完成后先跑对应测试，验证通过后再进入下一步
