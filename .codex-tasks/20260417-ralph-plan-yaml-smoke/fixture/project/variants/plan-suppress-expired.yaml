schema_version: "1.0.0"
tasks:
  - id: T1
    title: Suppress Expired Boundary
    role: leaf
    type: backend
    claimed_paths:
      - src/app.py
    goal_behavior: Expired but well-formed suppress lease should warn, not fail schema.
    acceptance_criteria: The wrapper test command is accepted.
    verification:
      level: unit
      command: make test
      covers:
        tasks:
          - T1
suppress_instances:
  - code: W_SAMPLE_RULE
    owner: team-ralph
    expiry: "2020-01-01"
    review_after: "2020-01-01"

