schema_version: "1.0.0"
mystery_top_level: true
tasks:
  - id: T1
    title: Strict Unknown Field Boundary
    role: leaf
    type: backend
    claimed_paths:
      - src/app.py
    goal_behavior: Strict schema should reject unknown fields.
    acceptance_criteria: Load must fail before validation runs.
    verification:
      level: unit
      command: make test
      covers:
        tasks:
          - T1

