Python 3.9+ project packaged with setuptools. Code style is pragmatic and explicit: small handler functions, clear normalization/validation helpers, pydantic contract models at boundaries, YAML/JSONL persistence, and append-only event ledger for auditability. Naming is snake_case for functions/modules, simple classes for domain models, and operation-oriented handler naming such as handle_*, try_handle_*.